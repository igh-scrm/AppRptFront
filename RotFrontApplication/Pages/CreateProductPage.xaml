<Page x:Class="RotFrontApplication.Pages.CreateProductPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:RotFrontApplication.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CreateProductPage">

    <Grid>
        <StackPanel
            Width="400"
            Height="300"
            VerticalAlignment="Center"
            HorizontalAlignment="Center">
            <GroupBox Header="Выберите тип товара:">
                <ComboBox
                    Name="CmbType"/>
            </GroupBox>
            <GroupBox Header="Название:">
                <TextBox
                    Name="TxbName"/>
            </GroupBox>
            <GroupBox Header="Тип хранения:">
                <ComboBox
                    Name="CmbNs"/>
            </GroupBox>
            <Button
                Content="ДОБАВИТЬ"
                Name="BtnAdd"
                Click="BtnAdd_Click"
                Width="100"
                Height="20"
                Margin="10"
                Background="#0776A0"
                BorderBrush="Transparent"
                Foreground="White"/>
        </StackPanel>
        <Button
                Width="60"
                Height="20"
                VerticalAlignment="Top"
                HorizontalAlignment="Right"
                Margin="30"
                Content="Назад"
                Background="#0776A0"
                BorderBrush="Transparent"
                Foreground="White"
                Name="BtnBack"
                Click="BtnBack_Click"/>
    </Grid>
</Page>
